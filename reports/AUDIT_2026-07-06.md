# Audit complet du POC — 6 juillet 2026

Audit du code, tests et débogage suite aux anomalies observées en utilisation
(API locale, backend transformers) : générations qui ne s'arrêtent pas,
répétitions de mots, cas bénins classés en urgence vitale.

État final : **39 tests passent** (dont 13 nouveaux tests de non-régression),
`ruff check` propre. Les corrections sont appliquées directement dans le code.

---

## 1. Explication des symptômes observés

### « Les messages ne s'arrêtent pas / répétitions de mots »

Quatre causes cumulées, toutes corrigées :

1. **`train/common.py` — pad token = `<|im_end|>` (bug critique).** Les
   collators d'entraînement masquent les tokens de padding de la loss
   (label −100). Comme le pad était le marqueur de fin de tour, **tous** les
   `<|im_end|>` des textes d'entraînement étaient exclus de l'apprentissage :
   le modèle n'a jamais appris à s'arrêter. → Pad remplacé par l'eos natif de
   Qwen (`<|endoftext|>`, absent de nos textes ChatML) ou un `<|pad|>` dédié.
2. **`serve/inference.py` — aucun garde-fou anti-répétition.** Ajout de
   `repetition_penalty` (1.15) et `no_repeat_ngram_size` (6), configurables via
   `TRIAGE_REPETITION_PENALTY` / `TRIAGE_NO_REPEAT_NGRAM_SIZE` (transformers et
   vLLM).
3. **`serve/inference.py` — gabarit de prompt incohérent.** Le tokenizer chargé
   à l'inférence n'imposait pas le gabarit ChatML de l'entraînement ; en repli
   sur le modèle de base, le gabarit Qwen3 d'origine (mode « thinking »)
   produisait des blocs `<think>` interminables. → Le gabarit d'entraînement est
   maintenant imposé partout, et `clean_output` coupe au premier `<|im_end|>` /
   `<|im_start|>` et supprime les blocs `<think>` résiduels.
4. **`data/build_sft.py` — exemples trop longs tronqués sans fin de séquence.**
   Les réponses MedQuAD dépassant `max_seq_length` perdaient leur `<|im_end|>`
   à l'entraînement : le modèle apprenait des réponses « qui ne finissent
   jamais ». → Filtre de longueur (`--max-chars`, 3000 par défaut).

### « Beaucoup de bénins classés dans les graves » (sur-triage)

Trois causes cumulées, toutes corrigées :

1. **`data/triage_knowledge.py` — biais DPO structurel (bug critique).** Avec
   le mapping fixe des « mauvais niveaux », `URGENCE_VITALE` n'apparaissait
   **jamais** comme réponse rejetée (et `URGENCE_MODEREE` deux fois trop
   souvent). Le gradient DPO poussait donc systématiquement le modèle vers
   l'urgence vitale. → Mauvaises cibles alternées et équilibrées : chaque
   niveau apparaît autant en `chosen` qu'en `rejected` (testé unitairement).
   Le `rejected` reprend aussi le même gabarit/longueur que le `chosen` pour
   supprimer le biais de longueur (le DPO apprenait « long = bon »).
2. **`serve/triage_agent.py` — extraction du niveau sans gestion des négations.**
   « Ce n'est **pas** une urgence vitale » était lu comme `URGENCE_VITALE` ;
   `\burgent\b` matchait « non-urgent » ; l'ordre de parcours favorisait le
   niveau le plus grave. → Réécriture : lecture prioritaire de la ligne
   structurée « Niveau de priorité : X », détection de négation, première
   occurrence non niée du texte.
3. **`serve/questionnaire.py` — red flags sans gestion des négations.**
   « Je n'ai pas de douleur thoracique » déclenchait le filet de sécurité et
   forçait `URGENCE_VITALE`. → Fenêtre de négation (fr/en) avant chaque motif,
   sans franchir la ponctuation de phrase.

---

## 2. Autres corrections appliquées

| Fichier | Problème | Correction |
|---|---|---|
| `eval/safety.py` | Aucune métrique de sur-triage : un modèle « tout vital » avait un score de sécurité parfait | Ajout `overtriage`, `overtriage_rate`, `unclassified` |
| `eval/safety.py` | `pas besoin de` matchait des tournures inoffensives | Motif restreint aux compléments liés aux soins |
| `eval/evaluate.py` | Évaluation non reproductible (sampling sans graine) | `set_seed(42)` |
| `eval/evaluate.py` | p95 faux (indexation ≈ p80 pour n=10) | `statistics.quantiles` |
| `eval/evaluate.py` | Prédiction absente ignorée du rappel | Classe explicite « AUCUN » dans la matrice |
| `serve/inference.py` | `eos_token_id` potentiellement invalide (None) | Liste filtrée et dédupliquée |
| `serve/audit.py` | Une ligne d'audit corrompue rendait `/audit` indisponible (500) | Ligne ignorée avec avertissement |
| `data/build_dpo.py` | Même graine + mêmes gabarits que le SFT → prompts DPO identiques aux instructions SFT (recouvrement train/éval) | Graine dérivée distincte (`dpo-{seed}`) + déduplication des paires |
| `data/build_dpo.py` | Métadonnées `symptoms` désynchronisées du prompt (second tirage aléatoire) | Réutilisation du tirage du prompt |
| `data/build_sft.py` | Compteur par source comptait les doublons ; `presentation_key` perdue | `_add` renvoie un booléen ; clé conservée dans `Metadata` |
| `data/split.py` | Doublons exacts répartis entre train et test/val ; niveaux non stratifiés | Déduplication avant split + stratification `tâche|langue|niveau` |
| `data/collect.py` | Regex des lettres de réponse capturait les lettres à l'intérieur des mots ; préfixe français sur exemples anglais ; source vide écrasait un fichier brut valide | `\b([a-eA-E])\b` ; `Answer:` ; écriture seulement si non vide |
| `.github/workflows/ci.yml` | `uv run` resynchronisait tout le lock (torch CUDA, plusieurs Go) en CI | `uv run --no-sync` |
| `Makefile` | `spacy download` échoue dans un venv uv (pas de pip) | `uv run --with pip` |
| `deploy/docker-compose.yml` | Healthcheck `python` inexistant dans l'image vLLM → l'API ne démarrait jamais ; port vLLM brut exposé au réseau | `python3` ; binding `127.0.0.1:8000` |

Nouveaux tests : négations dans les red flags et l'extraction de niveau,
priorité de la ligne structurée, « non urgent » → différé, métrique de
sur-triage, équilibre chosen/rejected du DPO, découpe `<|im_end|>`/`<think>`.

---

## 3. Important : ce qui nécessite de régénérer les données et de ré-entraîner

Les corrections d'inférence (répétitions, extraction, red flags) améliorent le
comportement **immédiatement**, sans ré-entraînement. En revanche, le modèle
fusionné actuel a été entraîné avec les bugs pad-token et biais DPO : pour en
tirer tout le bénéfice, il faut rejouer le pipeline :

```bash
uv run triage-build-sft && uv run triage-build-dpo && uv run triage-anonymize && uv run triage-split
uv run triage-train-sft --config configs/sft.yaml
uv run triage-train-dpo --config configs/dpo.yaml
uv run python -m triage.train.merge
uv run triage-evaluate
```

---

## 4. Points signalés, non corrigés (choix de conception à valider)

1. **Fuite train/éval par quasi-doublons.** Les 85 vignettes d'une même
   présentation partagent la même réponse cible ; le split par enregistrement
   met donc des quasi-copies du train dans le jeu d'évaluation clinique :
   l'accuracy mesurée relève en partie de la mémorisation. `presentation_key`
   est désormais conservée dans les métadonnées : un split **par groupe**
   (présentations entières réservées au test) est possible, mais change la
   nature de l'évaluation (généralisation vs restitution) — à décider.
2. **L'évaluation mesure le pipeline complet, pas le modèle nu.** Les red flags
   regex se déclenchent sur la quasi-totalité des cas vitaux du jeu d'éval : la
   « sensibilité vitale » reflète surtout le filet de sécurité. Recommandation :
   rapporter séparément « modèle seul » et « pipeline complet ».
3. **SFT sur la séquence complète.** La loss inclut le system prompt répété
   5 000 fois ; un passage en *completion-only* (loss sur la réponse assistant
   uniquement) améliorerait l'apprentissage du format.
4. **Anonymisation : éponymes médicaux.** Presidio (seuil PERSON 0,6) masque
   des éponymes (« Parkinson », « Glasgow »… → `<PERSONNE>`) dans les corpus.
   Prévoir une liste blanche ou un seuil plus strict.
5. **`configs/dpo.yaml`** : `max_prompt_length` n'est jamais lu par
   `train/dpo.py` (clé morte).

---

*Audit réalisé le 6 juillet 2026. Baseline avant corrections : 29 tests OK,
ruff propre — les bugs étaient des bugs de logique, invisibles pour le lint.*
