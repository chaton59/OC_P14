# Dépendances MINIMALES de la passerelle API (mode backend = vLLM).
# La passerelle n'exécute pas le modèle elle-même : elle relaie les requêtes
# vers le serveur vLLM via HTTP. Inutile donc d'embarquer torch/transformers,
# ce qui garde l'image légère.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.4
httpx>=0.27
pyyaml>=6.0
